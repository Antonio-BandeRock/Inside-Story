// Auto-generated by scripts/build_food_reference_db.py (bumped manually here,
// 2026-08-08, after directly patching the compiled .db a fourth time the
// same day for Graves' Disease, this app's fourth real condition: a new
// `conditions` row flipped to 'in_progress', 3 new `common_medications` rows
// (methimazole, propylthiouracil, propranolol), 3 new `interaction_rules`
// rows (agranulocytosis warning signs, PTU liver-injury warning signs, and
// antithyroid-drug/iodine efficacy, all reference-only), and 2 new
// `sub_criterion_condition_relevance` rows for condition_code='graves' --
// reusing Selenium (real RCT evidence for mild Graves' orbitopathy) and
// Iodine (reframed toward its excess-risk side, the opposite emphasis from
// Hashimoto's own deficiency-inclusive framing on the identical sub-
// criterion and tier data). Deliberately did NOT reuse Goitrogenic Load or
// the Mediterranean-pattern sub-criteria for Graves' -- no real, citable
// primary-source evidence was found supporting either reuse this session,
// and forcing a citation that doesn't really support the claim was judged
// worse than a smaller, more honest reuse set (the same standard RA's own
// "Irritants" exclusion already established) -- since no Python interpreter
// was available in this session to run a real rebuild) -- do not edit by
// hand.
export const REFERENCE_DB_VERSION = "20260808070000";
