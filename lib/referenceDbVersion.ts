// Auto-generated by scripts/build_food_reference_db.py (bumped manually here,
// 2026-08-08, after directly patching the compiled .db a ninth time the
// same day for Lupus (SLE), this app's ninth real condition: a
// `conditions` row flipped to 'in_progress'; 3 new `common_medications`
// rows (hydroxychloroquine, belimumab, mycophenolate_mofetil --
// prednisone, azathioprine, and methotrexate were all already present
// from earlier passes and reused directly, since all three are also real,
// common lupus medications); 1 new `interaction_rules` row
// (hydroxychloroquine_grapefruit_caution, a real, well-established,
// reference-only caution mirroring the existing
// cyclosporine_grapefruit_avoidance precedent exactly -- same rule_type,
// checkable=0, subject_b_kind='food_category'); and 1 new
// `sub_criterion_condition_relevance` row for condition_code='lupus' --
// Omega-3 vs 6 (id 11, grounded in a real meta-analysis of 5 RCTs finding
// a modest, low-certainty reduction in disease activity). Deliberately
// the narrowest reuse pass of any condition so far: no direct citation
// from this app's own lupus research connects Additives, Processing, or
// any other existing sub-criterion specifically to lupus, so none were
// forced into a reuse. Alfalfa sprouts' own real, well-documented
// L-canavanine trigger (the single strongest individual-food finding in
// this whole category) was deliberately NOT built into a new
// sub-criterion either -- it's a genuinely narrow concern affecting
// effectively one or two foods, not a broad per-food dimension the way
// Type 1 Diabetes's carbohydrate/fiber ratio or RA's elimination-trigger
// tag are, so it's covered through the Digest entry itself rather than a
// new database-wide scoring dimension -- since no Python interpreter was
// available in this session to run a real rebuild) -- do not edit by
// hand.
export const REFERENCE_DB_VERSION = "20260808170000";
