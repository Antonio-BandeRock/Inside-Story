// Auto-generated by scripts/build_food_reference_db.py (bumped manually here,
// 2026-08-08, after directly patching the compiled .db an eleventh time
// the same day for PCOS, this app's eleventh real condition and its FIRST
// genuinely non-autoimmune one: a `conditions` row flipped to
// 'in_progress'; 3 new `common_medications` rows (spironolactone,
// letrozole, clomiphene -- metformin was already present from earlier
// passes and reused directly, since it's also a real, first-line PCOS
// insulin-sensitizing medication); 1 new `interaction_rules` row
// (spironolactone_potassium_caution, a real, well-established,
// reference-only caution -- spironolactone's own aldosterone-blocking
// mechanism reduces potassium clearance, carrying a real hyperkalemia
// risk when combined with potassium supplements/salt substitutes/large
// potassium intake); and 1 new `sub_criterion_condition_relevance` row for
// condition_code='pcos' -- Carbohydrate Density Relative to Fiber (id 27,
// the sub-criterion built for Type 1 Diabetes on 2026-08-08 from real
// measured carbohydrate/fiber_total values). Arguably the single most
// naturally fitting reuse of any condition so far: PCOS's own central
// mechanism (insulin resistance, a real case-control study found it in
// 42.6% of PCOS patients vs. 17.1% of controls) is literally the same
// carbohydrate/insulin axis this sub-criterion already measures, so no
// new sub-criterion was needed at all. Omega-3 vs 6 and Additives/
// Processing were both considered and declined -- no direct citation from
// this session's own PCOS research connects either specifically to PCOS
// -- since no Python interpreter was available in this session to run a
// real rebuild) -- do not edit by hand.
export const REFERENCE_DB_VERSION = "20260808190000";
